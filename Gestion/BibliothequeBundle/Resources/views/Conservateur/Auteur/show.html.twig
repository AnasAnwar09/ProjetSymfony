{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Auteur</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nomauteur</th>
                <td>{{ entity.nomAuteur }}</td>
            </tr>
            <tr>
                <th>Prenomauteur</th>
                <td>{{ entity.prenomAuteur }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('auteur') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('auteur_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
